// GENERATED FILE — do not hand-edit.
//
// Regenerated automatically by .github/workflows/migrate.yml on every
// migration change (`supabase gen types typescript --linked`), and consumed
// by both the frontend and the Edge Functions. This placeholder exists so
// the repo has something to import against before a live Supabase project
// exists; once one is linked, run the command below locally to replace it:
//
//   supabase gen types typescript --linked > packages/shared-types/database.types.ts

export type Database = Record<string, never>;
